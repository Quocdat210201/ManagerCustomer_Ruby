<%= form_with(model: thuocnam) do |form| %>
  <% if thuocnam.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(thuocnam.errors.count, "error") %> prohibited this thuocnam from being saved:</h2>

      <ul>
        <% thuocnam.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <p>Số thứ tự</p>
    <%= form.number_field :stt %>
  </div>

  <div>
    <p>Tên</p>
    <%= form.text_field :ten %>
  </div>

  <div>
    <p>Đặc điểm</p>
    <%= form.number_field :dacdiem %>
  </div>

  <div>
    <p>Màu lá</p>
    <%= form.text_field :maula %>
  </div>

  <div>
    <p>Dược tính</p>
    <%= form.text_field :duoctinh %>
  </div>

  <div>
    <p>Lưu ý sử dụng</p>
    <%= form.text_field :luuysudung %>
  </div>

  <div>
    <p>Nơi trồng</p>
    <%= form.text_field :noitrong %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
